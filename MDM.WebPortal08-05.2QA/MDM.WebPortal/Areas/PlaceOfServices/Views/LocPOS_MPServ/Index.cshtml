@model IEnumerable<MDM.WebPortal.Models.FromDB.LocPOS_MPServ>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LocationsPOS.PosName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MPService.ServName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LocationsPOS.PosName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MPService.ServName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocPosMPServID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocPosMPServID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocPosMPServID })
        </td>
    </tr>
}

</table>
